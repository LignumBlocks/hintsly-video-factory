########################
# Dominio / Traefik
########################

# Email para Let's Encrypt (certificados SSL)
SSL_EMAIL=tu-email@ejemplo.com

# Subdominio donde vive n8n (ej: "n8n" -> n8n.tudominio.com)
SUBDOMAIN=n8n

# Dominio principal (sin subdominio)
DOMAIN_NAME=tudominio.com

# Timezone para n8n (usar IANA TZ, ej: "Europe/Madrid", "America/Bogota")
GENERIC_TIMEZONE=Europe/Madrid


########################
# Hintsly Engine
########################

# Clave de la API de Gemini / Nano Banana / Imagen (según proveedor)
GEMINI_API_KEY=TU_GEMINI_API_KEY_AQUI

# Clave de la API de Veo / Flow (cuando la tengas)
VEO_API_KEY=TU_VEO_API_KEY_AQUI

# Ruta de assets DENTRO del contenedor (no cambiar salvo que sepas lo que haces)
ASSETS_PATH=/assets/videos

# Puerto interno del Engine (coincide con el CMD de uvicorn)
ENGINE_PORT=8000

# Nivel de logs del Engine: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
